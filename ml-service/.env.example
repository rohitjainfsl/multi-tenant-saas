# Copy this file to .env for local development
# In Azure, set these under App Service -> Configuration -> Application settings

# Origin allowed to call this API (your Node backend's URL in production)
ALLOWED_ORIGIN=http://localhost:5000,https://multi-tenant-saas-backend.azurewebsites.net

# Port (Azure sets this automatically - only needed for local dev override)
PORT=8000
